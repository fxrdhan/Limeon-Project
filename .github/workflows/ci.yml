name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  # 🔍 Detect what files changed to optimize CI runs
  detect-changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      critical: ${{ steps.filter.outputs.critical }}
      needs_build: ${{ steps.filter.outputs.needs_build }}
      config: ${{ steps.filter.outputs.config }}
      docs: ${{ steps.filter.outputs.docs }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check for file changes
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            # Critical changes that need typecheck + lint + test (but not always build)
            critical:
              - 'src/**'
              - 'scripts/**'
              - 'public/**'
              - 'package.json'
              - 'yarn.lock'
              - '.yarnrc.yml'
              - 'tsconfig*.json'
              - 'vite.config.*'
              - 'vitest.config.*'
              - '.swcrc'
              - 'index.html'
              - '.env'
              - '.env.*'
              - '.github/workflows/**'

            # Subset of critical that actually needs BUILD (excludes test files, scripts, workflows)
            needs_build:
              - 'src/**/!(*.test).{ts,tsx,scss,css}'
              - 'src/**/*.{jpg,jpeg,png,gif,svg,webp}'
              - 'public/**'
              - 'package.json'
              - 'yarn.lock'
              - '.yarnrc.yml'
              - 'tsconfig*.json'
              - 'vite.config.*'
              - '.swcrc'
              - 'index.html'
              - '.env'
              - '.env.*'
              # NOTE: .github/workflows/** excluded - workflow changes don't affect app build

            # Config-only changes that need lint + format only (skip tests & build)
            config:
              - '.mergify.yml'
              - '.github/dependabot.yml'
              - '.prettierrc*'
              - '.prettierignore'
              - 'eslint.config.*'
              - '.eslintrc*'
              - '.eslintignore'
              - 'tailwind.config.*'
              - 'postcss.config.*'
              - '.husky/**'
              - '.gitignore'
              - '.gitattributes'
              - '.mcp.json'
              - '.mcp.json.example'
              - '*.example'

            # Docs & dev-only changes that need format check only (or skip entirely)
            docs:
              - '**/*.md'
              - 'LICENSE'
              - '.github/ISSUE_TEMPLATE/**'
              - '.github/PULL_REQUEST_TEMPLATE.md'
              - 'supabase/**'

  # ⚡ Fast checks - run in parallel for quick feedback
  typecheck:
    name: TypeScript Check
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.critical == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --immutable

      - name: Check TypeScript
        run: yarn tsc -b --noEmit

  lint:
    name: Lint Code
    runs-on: ubuntu-latest
    needs: detect-changes
    if: |
      needs.detect-changes.outputs.critical == 'true' ||
      needs.detect-changes.outputs.config == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --immutable

      - name: Lint code
        run: yarn lint

  format:
    name: Check Formatting
    runs-on: ubuntu-latest
    needs: detect-changes
    if: |
      needs.detect-changes.outputs.critical == 'true' ||
      needs.detect-changes.outputs.config == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --immutable

      - name: Check formatting
        run: yarn format:check

  # 🧪 Tests with coverage - runs after quick checks pass
  test:
    name: Tests & Coverage
    runs-on: ubuntu-latest
    needs: [detect-changes, typecheck, lint, format]
    if: |
      always() &&
      needs.detect-changes.outputs.critical == 'true' &&
      !contains(needs.*.result, 'failure') &&
      !contains(needs.*.result, 'cancelled')
    permissions:
      contents: read
      checks: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --immutable

      - name: Run tests with coverage
        run: yarn test:coverage

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage/coverage-final.json
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: ${{ github.actor != 'dependabot[bot]' }}

  # 🏗️ Build - runs after all checks pass (only if build-affecting files changed)
  build:
    name: Build Application
    runs-on: ubuntu-latest
    needs: [detect-changes, test]
    if: |
      always() &&
      needs.detect-changes.outputs.needs_build == 'true' &&
      !contains(needs.*.result, 'failure') &&
      !contains(needs.*.result, 'cancelled')
    permissions:
      contents: read
      actions: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --immutable

      - name: Build application
        run: yarn build
        env:
          # IDE warnings about these secrets are expected and safe to ignore
          # They occur because the VSCode extension can't verify secrets
          # The workflow will work correctly when secrets are configured
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build
          path: dist/
          retention-days: 7

  # ⏭️ Skip build for test-only or script-only changes
  skip-build:
    name: Build Application
    runs-on: ubuntu-latest
    needs: [detect-changes, test]
    if: |
      always() &&
      needs.detect-changes.outputs.critical == 'true' &&
      needs.detect-changes.outputs.needs_build == 'false' &&
      !contains(needs.*.result, 'failure') &&
      !contains(needs.*.result, 'cancelled')
    steps:
      - name: Build not needed
        run: |
          echo "⏭️ Build skipped - no build-affecting files changed"
          echo ""
          echo "Changed files don't affect production build:"
          echo "  • Test files (*.test.ts, *.test.tsx)"
          echo "  • Scripts (scripts/**)"
          echo "  • Workflows (.github/workflows/**)"
          echo "  • Type definitions (*.d.ts)"
          echo ""
          echo "✅ All checks passed (typecheck, lint, test)"
          echo "⚡ Saved ~2 minutes of CI time by skipping build!"

  # 📝 Lightweight check for docs & dev-only changes
  docs-check:
    name: Documentation & Dev Files Check
    runs-on: ubuntu-latest
    needs: detect-changes
    if: |
      needs.detect-changes.outputs.docs == 'true' &&
      needs.detect-changes.outputs.critical == 'false' &&
      needs.detect-changes.outputs.config == 'false'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --immutable

      - name: Check formatting
        run: yarn format:check

      - name: Success message
        run: |
          echo "✅ Documentation/dev files changes look good!"
          echo "📝 Changed: docs, supabase (local dev), or other non-production files"
          echo "⚡ Skipped heavy checks (typecheck, test, build) to save CI minutes"

  # ⚙️ Lightweight check for config-only changes
  config-check:
    name: Configuration Check
    runs-on: ubuntu-latest
    needs: detect-changes
    if: |
      needs.detect-changes.outputs.config == 'true' &&
      needs.detect-changes.outputs.critical == 'false'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --immutable

      - name: Lint and format check
        run: |
          yarn lint
          yarn format:check

      - name: Success message
        run: |
          echo "✅ Configuration changes look good!"
          echo "⚡ Skipped heavy checks (typecheck, test, build) to save CI minutes"
          echo "📋 Only ran lint + format check since no source code changed"
