name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  # 🔍 Detect what files changed to optimize CI runs
  detect-changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    # Skip CI for merge commits (already tested in PR)
    if: |
      !(
        github.event_name == 'push' &&
        github.ref == 'refs/heads/main' &&
        (
          contains(github.event.head_commit.message, 'Merge pull request') ||
          contains(github.event.head_commit.message, ' (#')
        )
      )
    outputs:
      critical: ${{ steps.filter.outputs.critical }}
      needs_build: ${{ steps.filter.outputs.needs_build }}
      config: ${{ steps.filter.outputs.config }}
      docs: ${{ steps.filter.outputs.docs }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Check for file changes
        uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            # Critical changes that need typecheck + lint + test (but not always build)
            critical:
              - 'src/**'
              - 'shared/**'
              - 'scripts/**'
              - 'public/**'
              - 'supabase/functions/**'
              - 'supabase/migrations/**'
              - 'package.json'
              - 'bun.lock'
              - 'tsconfig*.json'
              - 'vite.config.*'
              - 'vitest.config.*'
              - '.swcrc'
              - 'index.html'
              - '.env'
              - '.env.*'
              - '.github/workflows/**'

            # Subset of critical that actually needs BUILD (excludes test files, scripts, workflows)
            needs_build:
              - 'src/**/!(*.test).{ts,tsx,scss,css}'
              - 'shared/**/*.ts'
              - 'src/**/*.{jpg,jpeg,png,gif,svg,webp}'
              - 'public/**'
              - 'package.json'
              - 'bun.lock'
              - 'tsconfig*.json'
              - 'vite.config.*'
              - '.swcrc'
              - 'index.html'
              - '.env'
              - '.env.*'
              # NOTE: .github/workflows/** excluded - workflow changes don't affect app build

            # Config-only changes that need lint + format only (skip tests & build)
            config:
              - '.mergify.yml'
              - '.github/dependabot.yml'
              - '.oxfmtrc*'
              - '.oxfmtignore'
              - '.oxlintrc*'
              - 'tailwind.config.*'
              - 'postcss.config.*'
              - '.husky/**'
              - '.gitignore'
              - '.gitattributes'
              - '.mcp.json'
              - '.mcp.json.example'
              - '*.example'
              - '.vscode/**'
              - '.zed/**'
              - '.editorconfig'
              - '.nvmrc'
              - '.lintstagedrc*'
              - 'commitlint.config.*'

            # Docs & dev-only changes that need format check only (or skip entirely)
            docs:
              - '**/*.md'
              - 'LICENSE'
              - '.github/ISSUE_TEMPLATE/**'
              - '.github/PULL_REQUEST_TEMPLATE.md'

  # ⚡ Fast checks - run in parallel for quick feedback
  typecheck:
    name: TypeScript Check
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.critical == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Check TypeScript
        run: bunx --bun tsc -b --noEmit

      - name: Check chat schema drift
        run: bun run check:chat-schema

  lint:
    name: Lint Code
    runs-on: ubuntu-latest
    needs: detect-changes
    if: |
      needs.detect-changes.outputs.critical == 'true' ||
      needs.detect-changes.outputs.config == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint code
        run: bun run lint

  format:
    name: Check Formatting
    runs-on: ubuntu-latest
    needs: detect-changes
    if: |
      needs.detect-changes.outputs.critical == 'true' ||
      needs.detect-changes.outputs.config == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Check formatting
        run: bun run format:check

  # 🧪 Tests with coverage - runs after quick checks pass
  test:
    name: Tests & Coverage
    runs-on: ubuntu-latest
    needs: [detect-changes, typecheck, lint, format]
    if: |
      always() &&
      needs.detect-changes.outputs.critical == 'true' &&
      !contains(needs.*.result, 'failure') &&
      !contains(needs.*.result, 'cancelled')
    permissions:
      contents: read
      checks: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run tests with coverage
        run: bun run test:coverage
        env:
          VITE_SUPABASE_URL: https://example.com
          VITE_SUPABASE_ANON_KEY: dummy

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage/coverage-final.json
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: ${{ github.actor != 'dependabot[bot]' }}

  # 🏗️ Build - always runs, but skips actual build if not needed
  build:
    name: Build Application
    runs-on: ubuntu-latest
    needs: [detect-changes, test]
    if: |
      always() &&
      (needs.detect-changes.outputs.critical == 'true' || needs.detect-changes.outputs.needs_build == 'true') &&
      !contains(needs.*.result, 'failure') &&
      !contains(needs.*.result, 'cancelled')
    permissions:
      contents: read
      actions: write
    steps:
      - name: Check if build is needed
        id: check
        run: |
          if [ "${{ needs.detect-changes.outputs.needs_build }}" == "true" ]; then
            echo "needed=true" >> $GITHUB_OUTPUT
          else
            echo "needed=false" >> $GITHUB_OUTPUT
          fi

      - name: Skip build message
        if: steps.check.outputs.needed == 'false'
        run: |
          echo "⏭️ Build skipped - no build-affecting files changed"
          echo ""
          echo "Changed files don't affect production build:"
          echo "  • Test files (*.test.ts, *.test.tsx)"
          echo "  • Scripts (scripts/**)"
          echo "  • Workflows (.github/workflows/**)"
          echo ""
          echo "✅ All checks passed (typecheck, lint, test)"
          echo "⚡ Saved ~2 minutes of CI time!"

      - name: Checkout code
        if: steps.check.outputs.needed == 'true'
        uses: actions/checkout@v6

      - name: Setup Bun
        if: steps.check.outputs.needed == 'true'
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Node
        if: steps.check.outputs.needed == 'true'
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install dependencies
        if: steps.check.outputs.needed == 'true'
        run: bun install --frozen-lockfile

      - name: Build application
        if: steps.check.outputs.needed == 'true'
        run: bun run build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
          VITE_AG_GRID_LICENSE_KEY: ${{ secrets.VITE_AG_GRID_LICENSE_KEY }}

      - name: Upload build artifacts
        if: steps.check.outputs.needed == 'true'
        uses: actions/upload-artifact@v7
        with:
          name: build
          path: dist/
          retention-days: 7

  # 📝 Lightweight check for docs & dev-only changes
  docs-check:
    name: Documentation & Dev Files Check
    runs-on: ubuntu-latest
    needs: detect-changes
    if: |
      needs.detect-changes.outputs.docs == 'true' &&
      needs.detect-changes.outputs.critical == 'false' &&
      needs.detect-changes.outputs.config == 'false'
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Check formatting
        run: bun run format:check

      - name: Success message
        run: |
          echo "✅ Documentation/dev files changes look good!"
          echo "📝 Changed: docs or other non-runtime files"
          echo "⚡ Skipped heavy checks (typecheck, test, build) to save CI minutes"

  # ⚙️ Lightweight check for config-only changes
  config-check:
    name: Configuration Check
    runs-on: ubuntu-latest
    needs: detect-changes
    if: |
      needs.detect-changes.outputs.config == 'true' &&
      needs.detect-changes.outputs.critical == 'false'
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint and format check
        run: |
          bun run lint
          bun run format:check

      - name: Success message
        run: |
          echo "✅ Configuration changes look good!"
          echo "⚡ Skipped heavy checks (typecheck, test, build) to save CI minutes"
          echo "📋 Only ran lint + format check since no source code changed"
