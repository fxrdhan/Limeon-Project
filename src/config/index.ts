// Development configuration
interface Config {
  toast_tester_enabled: boolean;
  random_item_generator_enabled: boolean;
}

export const config: Config = {
  toast_tester_enabled: false,
  random_item_generator_enabled: true,
};

export default config;
