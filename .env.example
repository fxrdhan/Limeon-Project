# -----------------------------------------------------------------------------
# Environment Variables for PharmaSys Application
#
# Create a .env file in the root directory by copying this file and fill in
# the required values for your Supabase project.
#
# cp .env.example .env
# -----------------------------------------------------------------------------

# --- Supabase Frontend (Vite) ---
# These variables are exposed to the client-side application (prefixed with VITE_).
# They can be found in your Supabase project's "Project Settings" > "API" section.
#
# The public URL for your Supabase project.
VITE_SUPABASE_URL="https://your-project-ref.supabase.co"
#
# The anonymous, public-facing API key for your Supabase project.
VITE_SUPABASE_ANON_KEY="your-supabase-anon-key"

# --- Supabase Backend Scripts (Node.js) ---
# These variables are used by the scripts in the `/scripts` directory.
# They should NOT be prefixed with VITE_ to avoid exposing them to the client.
#
# The public URL for your Supabase project (same as VITE_SUPABASE_URL).
SUPABASE_URL="https://your-project-ref.supabase.co"
#
# The secret "service_role" key for your Supabase project.
# This key has admin privileges and can bypass Row Level Security.
# KEEP THIS KEY SECRET and never expose it in the frontend application.
# Found in "Project Settings" > "API" section.
SUPABASE_SERVICE_KEY="your-supabase-service-role-key"

# --- AG Grid Enterprise License ---
# AG Grid Enterprise license key for using premium features.
# Get your trial/production key from: https://www.ag-grid.com/license-pricing
# For CI/CD, add this as a GitHub Secret: VITE_AG_GRID_LICENSE_KEY
VITE_AG_GRID_LICENSE_KEY="your-ag-grid-license-key"
